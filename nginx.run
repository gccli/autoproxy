#! /bin/bash

source openresty.rc

NGX_CONF=$(readlink -f nginx.conf)

if ! grep "$NGINX_PATH/sbin" ~/.profile; then
    echo export PATH='$PATH:'$NGINX_PATH/sbin >> ~/.profile
    source ~/.profile
fi

nginx -c $NGX_CONF
